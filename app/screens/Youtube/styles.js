import {StyleSheet} from 'react-native';
import {COLOR_LIGHT_BLACK} from '../../styles';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: COLOR_LIGHT_BLACK,
  },
  conYtb: {
    alignSelf: 'stretch',
    height: 300,
  },
});

export default styles;
